import { View } from 'react-native';
import RepositoryList from './RepositoryList';

const Main = () => {
    return (
        <View style={{ padding: 40 }}>
            <RepositoryList />
        </View>
    );
};

export default Main;